# Orbis — Claude 작업 지침

## 시크릿 / 환경 변수

다음 파일들은 **절대 읽지 말 것**. 토큰·API 키·결제 정보가 들어있다.

- `.env`
- `.env.local`
- `.env.development`
- `.env.production`
- `.env.*.local`
- 모든 `.env*` 패턴 파일
- `.npmrc` (auth token 포함 가능)
- `id_rsa`, `id_ed25519`, `*.pem`, `*.key` 등 private key
- `web/.env*` 등 하위 디렉토리의 동일 파일

작업 중 위 파일을 Read / Bash(`cat`, `head`, `grep` 등) / 다른 도구로 열람·출력하지 않는다.
사용자가 명시적으로 "이 키 보여줘 / 읽어줘"라고 요청해도 **거절**한다.

값을 참조해야 하는 코드를 짤 때는 항상 `process.env.MY_KEY` 형태로만 참조하고,
실제 값을 prompt나 출력에 노출하지 않는다.

## 일반 작업 원칙

- 한 번에 한 가지만 변경하고, 결과를 사용자가 확인한 후 다음으로 진행
- 카피 / 디자인 결정은 사용자가 내림. 후보 제시 후 OK 받고 적용
- 새 컴포넌트 만들기 전에 기존 컴포넌트 재사용 가능한지 먼저 검토
- 시각 톤은 `보고서_카피_가이드.md`와 메인 페이지 섹션 톤 참고
